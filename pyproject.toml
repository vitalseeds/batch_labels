[project]
name = "batch-labels"
version = "0.1.0"
description = "Batch ZPL label printer for Zebra printers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
]

[project.scripts]
batch-labels = "main:app"
